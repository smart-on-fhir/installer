#!/bin/bash

VOLUME_ID="{{aws_ec2_volume_id}}"

# list snapshots
echo "\$(aws ec2 describe-snapshots --filters Name=volume-id,Values=$VOLUME_ID | jq -r '.Snapshots[]')"

echo "$(aws ec2 describe-snapshots --filters Name=volume-id,Values=$VOLUME_ID | jq -r '.Snapshots[]')"